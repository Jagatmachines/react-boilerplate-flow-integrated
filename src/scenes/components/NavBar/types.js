import * as React from 'react';

export type INavBarProps = {
  profileBar: React.Node,
  vehicleStatusBar: React.Node,
  toggleHamburger: Function,
  siteLogo: React.Node,
  notificationBar: React.Node,
  navSearch: React.Node
};
